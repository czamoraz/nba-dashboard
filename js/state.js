/* ================================================================
   state.js — Única fuente de verdad del estado de la app.
   Debe cargarse antes que api.js y main.js.
   ================================================================ */

"use strict";

window.AppState = {
  currentSeason: null,
  seasons:       [],
};
